cronJob: {
  schedule: .EveryFiveMinutes(),
  action: statistics.update(), // Consumes rabbitMQ's messages and updates the postgres db
}

routes: {
  '/top-queries' -> {
    query: string;
    percentage: number;
  }
  '/average-request-time' -> {
    averageTimeMs: number;
  }
  '/popular-time' -> {
    hour: string;
    requestCount: number;
  }
}

